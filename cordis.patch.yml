# dsh-ui-hud profile patch layer — insert one loader entry.
# The browser half is picked up by dsh-client-modules through the package's
# dsh.client declaration, exactly like the shipped ui-* packages.
- insert:
    - id: ui-hud
      name: 'dsh-ui-hud'
